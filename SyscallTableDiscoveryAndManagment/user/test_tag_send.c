#include "config.h"

int main(int argc, char** argv){
	
    char *buffer = "";

    printf("\n size of buffer is: %ld!", sizeof(""));

    printf("\n size of buffer is: %ld!", sizeof("a"));

    //int tag_descriptor = syscall(134,IPC_PRIVATE,CREATE,PERM_ALL);

	//syscall(156,tag_descriptor); 

	

}